/**
 * Affiliate / related-parts commerce (RFP area 9).
 * Intentionally empty — no outbound affiliate links in MVP.
 */
export function AffiliateSlot(_props: { productId?: string }) {
  return null;
}
